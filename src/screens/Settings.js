import React from 'react';
import { Text } from 'react-native';

import { Container } from '../components';

export default () => (
  <Container>
    <Text>
      Settings
    </Text>
  </Container>
);